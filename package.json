{
  "name": "bhairav-robotics-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx vite --root frontend",
    "build": "npx vite build --root frontend",
    "build:dev": "npx vite build --mode development --root frontend",
    "test": "cd frontend && npx vitest run",
    "lint": "cd frontend && npx eslint ."
  }
}