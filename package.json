{
  "name": "bhairav-robotics-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix frontend install --no-package-lock && npm --prefix frontend run dev",
    "build": "npm --prefix frontend install --no-package-lock && npm --prefix frontend run build",
    "build:dev": "npm --prefix frontend install --no-package-lock && npm --prefix frontend run build:dev",
    "test": "npm --prefix frontend run test",
    "lint": "npm --prefix frontend run lint"
  }
}
